# AGENTS.md

本文件是地理智能平台的开发协作入口，供 Codex 和仓库维护者使用。具体架构、设计与工具契约按需查阅下列文档。

## 协作方式

- 用户当前明确要求优先于本文件和技能中的默认工作流；系统指令、工具权限和真实授权边界仍然适用。
- 用户要求实现、修复或整理时，完成相关修改和验证后交付。用户只要求分析、审查或计划时，交付证据和建议。
- 已有授权在当前任务范围内持续有效。常规实现选择、只读调查和可逆的相关修改自行推进；只有缺失信息会实质改变结果或操作超出授权时才提问。提问前先完成不依赖该答案的工作。
- 用户中途补充、纠正或询问进展时，结合已有目标继续；只有明确取消或替换目标才放弃原任务。上下文压缩后从已完成进度继续。
- 修复从可复现原因入手。保护用户未提交改动，避免无关重构、全仓格式化、重置或覆盖；清理文件前核实用途与引用。
- 独立搜索和只读检查可并行。有边界明确、能节省时间或提高质量的独立子任务，且当前环境允许时，可委派子代理；分清文件所有权，由主代理整合验证。简单任务直接完成。
- 用简洁中文沟通，先说结果和证据，必要时解释原因。少用英文术语、重复标题和固定套话；最终说明改了什么、验证了什么、还有什么实际限制。
- 区分模型“词元”和身份“令牌”；供应商实际用量与估算分别标注。遇到失败如实说明，已有充分证据后及时交付。

## 开发技能与凭据

- 用户点名或任务确实需要时使用相应技能，先读其入口与必要引用；只加载相关资料。技能提供方法，不额外扩大任务、切换技术栈或增加交付物。
- 技能导致确认、暂停或偏离用户目标时，先核对适用条件和已有授权；确需暂停则链接具体 `SKILL.md`、引用条款并说明原因，同时推进不受影响的工作。
- 本项目的源码编辑、文档、构建和本地模拟测试不需要真实模型 API Key，不触发密钥创建、复用确认或凭据检查流程。
- 开发助手不读取或探测系统钥匙串、系统密钥、环境变量或 `.env*` 中的模型 API Key。真实模型联调仅使用用户明确授权的来源；缺少该授权时先完成本地模拟验证。
- 密钥、Cookie、认证令牌不得进入工具输出、日志、截图、报告、测试夹具、Git 或记忆。用户要求不读取的来源，即使只检查是否存在也不探测。
- 维护已有 Agents SDK 代码时沿用仓库的 TypeScript、依赖版本和启动链路。模型指南用于校准开发行为；更换产品模型、SDK、协议或部署方式需要相应任务授权。

## 基线与资料

开始修改前确认实际仓库根目录、Git 状态和相关入口；路径变化时重新定位。只读与本次修改有关的文档和代码，过时记忆、评审和计划中的目标先与当前实现核对。

| 修改范围 | 按需查阅 |
| --- | --- |
| 启动、开发命令 | [README](README.md)、[脚本索引](scripts/README.md)、相关 `package.json` |
| 模块边界、事实源 | [架构总览](docs/architecture/overview.md) |
| 运行、恢复、SDK 所有权 | [运行时 ADR](docs/architecture/adr-0001-agent-runtime-ownership.md)；待验收范围见[架构总览](docs/architecture/overview.md#待完成的稳定版验收) |
| 桌面与终端界面 | [设计规范](docs/standards/DESIGN.md) |
| 工具与跨语言契约 | [工具接入标准](docs/standards/tool-integration-standard.md)；示例目录另有局部约定 |
| 本机服务与 CLI | [运维手册](docs/operations/operations-console.md)、[对话 CLI](docs/operations/local-agent-cli.md) |

## 实现边界

### 代码与职责

- 保持 Electron + TypeScript + PostgreSQL/PostGIS + OpenAI Agents SDK；Python Worker 负责科学计算。应用在 `apps/`，共享能力在 `packages/`，源码放各包 `src/`。
- TypeScript 保持严格类型和 `noUncheckedIndexedAccess`；ESM 导入使用 `.js`。外部数据以 `unknown` 接收并经 Zod 校验，生产代码不用 `as any` 或裸断言绕过边界。
- 按资源与状态所有权组织模块；有状态服务显式注入依赖，不新增模块级可变单例、循环依赖或反向导入。拆分须解决具体耦合、测试或扩展问题，不以行数为目标。
- 通用组件优先复用现有成熟实现；领域能力经窄接口、注册表和适配器接入。删除被替代路径，避免无意义转发壳、别名、双写和静默旧数据兼容。
- 新源码沿用现有文件头和命名；人类作者与 AI 协助分开记录，日期与来源可追溯。保留历史署名和维护记录，注释解释职责、意图与边界。
- 产品代号只从 `packages/shared-types/src/productIdentity.ts` 读取。路径从入口位置或显式配置解析，不硬编码开发者主目录、检出目录或平台盘符。
- 依赖更新同步锁定文件，保留上游许可证和来源；第三方改动可追溯。新增脚本纳入 `scripts/README.md` 的现有调用入口，临时文件放临时目录。

### 协议与持久化

- 结构化平台事实由 PostgreSQL 保存；文件存储只保存大对象与载荷，数据库约束其引用和生命周期。缓存、UI、日志和 JSON/JSONL 文件不能成为第二事实源。
- 公共数据库 schema 来自 `packages/db`，跨表变更在事务内完成。普通 CRUD 使用 Drizzle；特殊 SQL 说明必要性。测试或排错不擅自重建用户数据库。
- HTTP 承载认证、文件、健康和查询；WS 承载运行控制、订阅与流式状态。两者经薄入口调用同一应用服务，跨边界有 schema、授权和稳定中文错误。
- WS 命令的请求、响应、认证和 CSRF 从 `packages/shared-types` 的 `wsCommandContracts` 派生。注册方提供命令名和处理器，授权与写资源策略通过注册器装配；不恢复各端手写协议副本。
- 保持工作区隔离、默认拒绝和副作用审批。错误、超时、拒绝及结构漂移真实可见，日志可定位且脱敏；不伪造结果或用吞错掩盖失败。
- 初始化存储后才监听服务；关闭由既有生命周期模块统一排空请求、队列并释放连接。本机服务由监督器管理，进程状态与健康来自真实句柄和探针。

### 架构与运行时

- 平台 `RunEngine` 拥有持久 Run/Turn、输入、审批和子运行生命周期；SDK `Runner` 独占单段模型—工具循环。同一 Run 同时最多一个活动段，独立子运行受并发、深度和预算限制。
- 仅使用 SDK 公开接口与恢复载荷；持久版本、配置摘要和上下文绑定在恢复前校验。不得解析 SDK 私有序列化布局或以尽力兼容跳过不匹配。
- 模型输入按消息身份组合，过滤与压缩不改写持久历史；工具调用、结果、推理和审批的协议组保持完整。摘要可追溯，固定系统指令与动态任务、地图、记忆分离。
- 工具间以 `valueRef` 传递派生数据，未知引用明确失败；Artifact 必须对应真实产物，展示面由契约声明。只读、写入、破坏性和审批语义按工具定义，不靠提示词绕过。
- 计划用于推进真实工作，不增加审批层；SDK、MCP、沙箱和子运行沿用各自生命周期。供应商能力按实际协议声明，外部 Agent tracing 保持关闭。
- 仓库开发指令不自动注入产品提示词，`instructionMemoryEnabled` 默认关闭。产品技能须显式配置、使用可信快照并遵守执行权限；安装开发技能不等于启用产品技能。

### 客户端与科学计算

- Electron Main 持有认证、网络、文件和窗口能力；Preload 仅暴露校验后的窄接口，Renderer 经统一桥调用，不接触密钥、任意 URL、绝对文件路径或 Node 对象。
- Zustand 保存跨功能实时状态，TanStack Query 保存 HTTP 查询缓存，表单草稿留在本地；组件精确订阅。共享对话投影归 `packages/conversation-presentation`，渲染期间不修改投影索引。
- 长列表、流式更新和加载优化按实际性能问题实施；沿用统一设计规范并验证键盘、减少动态效果和 macOS 标题栏。保留互动从当前位置直接到底的滚动行为。
- Worker 只做认证、路由、校验和调度；算法放 `packages/gis-meteorology`，重量级科学库惰性导入，业务读取复用既有读取入口。
- 科学变量、时间、单位与统计由 xarray 解释，地图栅格执行由 rasterio/GDAL 负责。核验 CRS、范围与缺测数据，结论可复核；缺失数据不编造。
- Worker 不持有会话事实或调用外部模型；保持 HMAC、防重放、请求限制、跨进程并发与可终止执行。文件只接受 `RUNTIME_ROOT` 内安全相对路径；不使用 `allow_pickle=True`、全局屏蔽警告或 `os.chdir` 绕过问题。

## 验证与交付

按本次改动的行为影响选择验证。用户明确要求的检查与发布门禁仍须完成；相同版本已有可靠结果可复用，无新修改、失败或未解疑点时不反复扩大检查。

| 改动 | 验证范围 |
| --- | --- |
| 文档、注释 | 差异、引用、命令与实际入口一致性；不为凑覆盖率新增测试或重打包 |
| 局部逻辑、普通 UI | 相关类型/规范检查和行为测试；UI 修改实际查看受影响页面 |
| 协议、数据库、权限、恢复、共享投影 | 相关单元、契约、失败路径与集成测试；跨模块补架构检查和受影响真实链路 |
| 桌面启动、安装、运行服务、打包 | 对应制品/发布测试，并在正式 Electron 包验证受影响启动或操作路径 |
| 发布或广泛跨包变更 | 完整 `npm test`、构建及相关 lint；涉及 Worker 跑 Python 测试，发布按目标平台打包验收 |

- 测试证明用户行为、契约和失败边界，不机械对应每个函数或重复实现。必要的回归用例随修复提交，保留有价值的已有测试。
- 单元测试不依赖真实模型、网络或用户数据；集成测试使用独立数据库与临时运行目录。模型链路优先使用本地兼容桩，科学计算使用小型可解释数据。
- 修改共享包后先构建相关依赖，再运行依赖其产物的定向测试。自动化通过、打包成功和实际界面验收分别报告，不相互替代。
- 交付时给出修改位置、实际检查结果和未完成原因；不把跳过项说成通过，不把历史计划状态当作本次验收。文档更新沿用现有入口并删除失效引用。

## 常用命令

从仓库根执行，完整入口以 `package.json`、[脚本索引](scripts/README.md)和 [CI](.github/workflows/ci.yml) 为准。只运行本次需要的命令。

| 用途 | 命令 |
| --- | --- |
| 首次安装锁定依赖 | `npm ci` |
| 对话 CLI / 运维台 / 桌面开发态 | `./dev.sh agent` / `./dev.sh` / `./dev.sh desktop` |
| 服务端 / 桌面测试 | `npm run test:server` / `npm run test:desktop` |
| 共享展示 / 监督器测试 | `npm run test:presentation` / `npm run test:supervisor` |
| 完整测试 / 构建 | `npm test` / `npm run build` |
| 桌面 / 术语检查 | `npm run lint:desktop` / `npm run lint:terminology` |
| 死代码 / 已构建桌面体积检查 | `npm run check:deadcode` / `npm run check:bundle` |
| 运行服务 / 发布契约测试 | `npm run test:runtime-release` / `npm run test:release-pipeline` |
| Python 测试 | `uv run --project apps/worker --extra test python -m pytest apps/worker/tests tests -q` |
| macOS arm64 正式包 | `npm run package:macos:arm64 --workspace @geo-agent-platform/desktop` |

本文件采用 [GPT-6 Astra 的开发协作建议](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra)：说明目标、授权、技能优先级、沟通风格和验证范围。保持入口简短，工程细节通过现有文档与代码契约维护，不新增重复规范。
